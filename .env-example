# This is an example .env file. Copy this to .env and fill in your actual values.
# Do NOT commit your actual .env file to Git.

# Nostr Private Keys (hex format)
# Your bot will be launched with a specific CSV file (e.g., events_en.csv or events_ru.csv)
# and an environment variable name that holds the corresponding private key.
# For example, if you run: ./nostr_bot events_en.csv NOSTR_PRIVATE_EN
# Then you need to define NOSTR_PRIVATE_KEY_EN here.
# Add as many private keys as you have language versions or separate bot identities.

NOSTR_PRIVATE_KEY_HEX_1="your_nostr_private_key_hex_for_first_identity_here"
NOSTR_PRIVATE_KEY_HEX_2="your_nostr_private_key_hex_for_second_identity_here"

# Example for English and Russian specific keys as per README:
# NOSTR_PRIVATE_KEY_EN="your_english_specific_private_key_hex"
# NOSTR_PRIVATE_KEY_RU="your_russian_specific_private_key_hex"

# Logging Configuration (Optional)

# LOG_DIR: Specifies a directory where log files should be stored.
# If not set, logs will be created in the directory where the bot is run.
# Example: LOG_DIR=./logs

# LOG_LEVEL: Set the desired logging level.
# Options: debug, info, warn, error
# Defaults to 'info' if not specified.
# 'debug' is very verbose and useful for troubleshooting.
LOG_LEVEL=info

# DEBUG: Alternative to LOG_LEVEL=debug. Set to 'true' for maximum debug output.
# Overrides LOG_LEVEL if set to 'true'.
# DEBUG=true

# CONSOLE_LOG: Set to 'true' to output logs to the console in addition to the log file.
# Useful for interactive sessions. Defaults to 'false' (file logging only).
# CONSOLE_LOG=true 